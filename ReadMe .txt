This is a dice rolling game where the player chooses to roll (y) or stop (n).
import random lets Python generate random numbers for the dice (1–6).

while True keeps the game running until the player stops.

If the input is invalid, it asks again.

Each roll prints the two dice results.
